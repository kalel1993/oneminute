'use client';import {useEffect,useState} from 'react';type Data={configured:boolean;leaders:{name:string;score:number}[];activity:{name:string;score:number;at:string}[]};
export function PublicData(){const[d,setD]=useState<Data|null>(null);useEffect(()=>{fetch('/api/public').then(r=>r.json()).then(setD).catch(()=>setD({configured:false,leaders:[],activity:[]}))},[]);return <section className="proof" aria-labelledby="activity-title"><div><p className="eyebrow">THE PACE</p>{d?.leaders?.length?<><strong className="record">{d.leaders[0].score}</strong><span>verified hits · world record</span></>:<><strong className="record">—</strong><span>{d?.configured?'No verified runs yet. Set the first pace.':'Local arcade mode · rankings come online with the database.'}</span></>}</div><div><h2 id="activity-title">LIVE ACTIVITY</h2>{d?.activity?.length?<ul className="feed">{d.activity.map((a,i)=><li key={i}><b>{a.name}</b><span>{a.score} hits</span></li>)}</ul>:<p className="empty">It’s quiet in here. The first verified run will appear live—maybe yours.</p>}</div></section>}
