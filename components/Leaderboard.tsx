'use client';import {useEffect,useState} from 'react';type Row={name:string;score:number};
export function Leaderboard(){const[mode,setMode]=useState<'touch'|'mouse'>('touch'),[period,setPeriod]=useState<'today'|'all'>('today'),[rows,setRows]=useState<Row[]|null>(null),[configured,setConfigured]=useState(true);useEffect(()=>{fetch(`/api/public?mode=${mode}&period=${period}`).then(r=>r.json()).then(d=>{setRows(d.leaders);setConfigured(d.configured)})},[mode,period]);return <><div className="tabs" aria-label="Leaderboard filters"><button className={period==='today'?'active':''} onClick={()=>setPeriod('today')}>LIVE / TODAY</button><button className={period==='all'?'active':''} onClick={()=>setPeriod('all')}>ALL TIME</button><button className={mode==='touch'?'active':''} onClick={()=>setMode('touch')}>TOUCH</button><button className={mode==='mouse'?'active':''} onClick={()=>setMode('mouse')}>MOUSE</button></div><div className="board" aria-live="polite">{rows?.length?rows.map((r,i)=><div className="row" key={`${r.name}-${i}`}><b>#{i+1}</b><span>{r.name}</span><b>{r.score}</b></div>):<div className="emptyBoard"><h2>{rows===null?'LOADING THE PACE…':'THE BOARD IS WIDE OPEN.'}</h2>{rows!==null&&<p>{configured?'No verified runs in this lane yet. Be the first name up.':'Rankings are offline until DATABASE_URL is provisioned. Local play remains available and unranked.'}</p>}</div>}</div></>}
