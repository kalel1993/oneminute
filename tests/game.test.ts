import {describe,expect,it} from 'vitest';import {outcome,percentile,stats,targetAt} from '@/lib/game/engine';import {validateTrace} from '@/lib/game/validation';
describe('deterministic targets',()=>{it('repeats and stays in the safe arena',()=>{expect(targetAt(42,3)).toEqual(targetAt(42,3));for(let i=0;i<150;i++){const t=targetAt(4,i);expect(t.x).toBeGreaterThanOrEqual(10);expect(t.x).toBeLessThanOrEqual(90);expect(t.y).toBeGreaterThanOrEqual(18);expect(t.y).toBeLessThanOrEqual(92)}});it('ramps target size down',()=>expect(targetAt(1,120).r).toBeLessThan(targetAt(1,0).r))});
describe('statistics',()=>{it('computes score, misses, streak, reaction and accuracy',()=>expect(stats([{type:'hit',t:200,x:1,y:1},{type:'hit',t:500,x:1,y:1},{type:'miss',t:700,x:1,y:1}])).toEqual({score:2,misses:1,streak:2,averageReaction:250,accuracy:67}));it('ranks percentile and challenge outcomes',()=>{expect(percentile(20,[10,20,30,40])).toBe(50);expect(percentile(4,[])).toBeNull();expect(outcome(5,4)).toBe('win');expect(outcome(4,5)).toBe('loss');expect(outcome(5,5)).toBe('tie')})});
describe('anti-cheat validation',()=>{it('accepts plausible deterministic hits',()=>{const a=targetAt(8,0),b=targetAt(8,1);expect(validateTrace(8,[{type:'hit',t:200,x:a.x,y:a.y},{type:'hit',t:500,x:b.x,y:b.y}],60000).valid).toBe(true)});it('rejects short, nonmonotonic, impossible and too-fast traces',()=>{const a=targetAt(8,0);const r=validateTrace(8,[{type:'hit',t:200,x:a.x,y:a.y},{type:'hit',t:210,x:0,y:0},{type:'miss',t:100,x:101,y:0}],1000);expect(r.valid).toBe(false);expect(r.reasons).toContain('invalid duration');expect(r.reasons).toContain('implausible hit rate');expect(r.reasons).toContain('non-monotonic timing')})});
describe('machine-generated traces',()=>{it('rejects perfectly repeated hit timing',()=>{const events=Array.from({length:25},(_,i)=>{const target=targetAt(12,i);return{type:'hit' as const,t:150+i*100,x:target.x,y:target.y}});const result=validateTrace(12,events,60000);expect(result.valid).toBe(false);expect(result.reasons).toContain('machine-like timing')});it('rejects events timestamped beyond the real session',()=>{const target=targetAt(3,0);const result=validateTrace(3,[{type:'hit',t:60000,x:target.x,y:target.y}],57000);expect(result.reasons).toContain('timing exceeds elapsed duration')})});

