import Link from 'next/link';import {notFound} from 'next/navigation';import type {Metadata} from 'next';import {eq} from 'drizzle-orm';import {getDb,hasDb} from '@/lib/db';import {challenges,players,sessions} from '@/lib/db/schema';
async function getChallenge(id:string){if(!hasDb())return null;const [row]=await getDb().select({name:players.displayName,score:sessions.score,valid:sessions.valid}).from(challenges).innerJoin(players,eq(players.id,challenges.creatorId)).innerJoin(sessions,eq(sessions.id,challenges.sessionId)).where(eq(challenges.id,id)).limit(1);return row?.valid?row:null}
export async function generateMetadata({params}:{params:Promise<{challengeId:string}>}):Promise<Metadata>{const{id}=await Promise.resolve({id:(await params).challengeId});const c=await getChallenge(id);return{title:c?`${c.name} scored ${c.score}. Beat it.`:'Challenge',description:'You have 60 seconds. Can you beat this verified Button Rush score?',alternates:{canonical:`/c/${id}`},openGraph:{images:[`/c/${id}/opengraph-image`]}}}
export default async function Page({params}:{params:Promise<{challengeId:string}>}){const{id}=await Promise.resolve({id:(await params).challengeId});if(!hasDb())return <main className="result"><p className="kicker">CHALLENGE UNAVAILABLE</p><h2>RANKINGS<br/><span>OFFLINE.</span></h2><p className="notice">This deployment needs DATABASE_URL and the migration before verified challenges can load.</p><Link className="action" href="/play">PLAY LOCALLY</Link></main>;const c=await getChallenge(id);if(!c)notFound();return <main className="result"><p className="kicker">A VERIFIED CHALLENGE FROM {c.name.toUpperCase()}</p><h2>BEAT<br/><span>{c.score}</span></h2><p className="lede">You get 60 seconds. Same game. No excuses.</p><Link className="action" href={`/play?challenge=${encodeURIComponent(id)}`}>ACCEPT CHALLENGE →</Link><Link className="fine" style={{marginTop:20}} href="/leaderboard">Check the leaderboard</Link></main>}
