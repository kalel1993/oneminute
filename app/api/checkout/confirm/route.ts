import {NextResponse} from 'next/server';import {z} from 'zod';import {identity,sameOrigin} from '@/lib/server';import {fulfilCheckout,stripeClient} from '@/lib/credits';
export async function POST(req:Request){if(!await sameOrigin())return NextResponse.json({error:'Cross-origin request refused'},{status:403});const parsed=z.object({sessionId:z.string().startsWith('cs_')}).safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Invalid checkout.'},{status:400});const player=await identity();if(!player.signedIn)return NextResponse.json({error:'Log in to confirm your credits.'},{status:401});const session=await stripeClient().checkout.sessions.retrieve(parsed.data.sessionId);if(session.metadata?.playerId!==player.id)return NextResponse.json({error:'This checkout belongs to another account.'},{status:403});const fulfilled=await fulfilCheckout(session);return NextResponse.json({fulfilled})}
